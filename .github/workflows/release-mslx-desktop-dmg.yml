name: 发布 MSLX-Desktop (macOS DMG包)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch: # 允许手动触发

permissions:
  contents: write

jobs:
  release-mac:
    runs-on: macos-latest
    name: 构建并发布 macOS DMG
    
    strategy:
      matrix:
        include:
          - { arch: x64,   rid: osx-x64,   bin: 'MSLX-Desktop' }
          - { arch: arm64, rid: osx-arm64, bin: 'MSLX-Desktop' }

    steps:
      - name: 签出代码
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: 设置 .NET 10 环境
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: 确定版本号 (Tag vs Manual)
        id: version
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            VERSION_NAME="${{ github.ref_name }}"
            BUNDLE_SHORT_VERSION="${VERSION_NAME#v}"
            BUNDLE_SHORT_VERSION="${BUNDLE_SHORT_VERSION%%-*}"
            echo "这是正式 Release 构建: ${{ github.ref_name }}"
          else
            VERSION_NAME="beta"
            BUNDLE_SHORT_VERSION=$(sed -n 's:.*<AssemblyVersion>\([^<]*\)</AssemblyVersion>.*:\1:p' ./MSLX.Desktop/MSLX.Desktop.csproj)
            echo "这是手动触发构建，使用版本号: beta"
          fi

          if [[ ! "$BUNDLE_SHORT_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(\.[0-9]+)?$ ]]; then
            echo "::error::CFBundleShortVersionString 格式不正确，当前值为: $BUNDLE_SHORT_VERSION"
            exit 1
          fi

          echo "VERSION_NAME=$VERSION_NAME" >> "$GITHUB_ENV"
          echo "BUNDLE_SHORT_VERSION=$BUNDLE_SHORT_VERSION" >> "$GITHUB_ENV"
          echo "BUNDLE_VERSION=${{ github.run_number }}" >> "$GITHUB_ENV"

      - name: 配置 NetSparkle 发布参数
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.NETSPARKLE_ED25519_PRIVATE_KEY }}
          SPARKLE_PUBLIC_KEY: ${{ secrets.NETSPARKLE_ED25519_PUBLIC_KEY }}
        run: |
          if [[ "$VERSION_NAME" == "beta" || -z "$SPARKLE_PRIVATE_KEY" || -z "$SPARKLE_PUBLIC_KEY" ]]; then
            echo "NETSPARKLE_ENABLED=false" >> "$GITHUB_ENV"
            echo "NETSPARKLE_APPCAST_URL=" >> "$GITHUB_ENV"
            echo "NETSPARKLE_ED25519_PUBLIC_KEY=" >> "$GITHUB_ENV"
            echo "::notice::NetSparkle 未启用；正式发布需要同时配置 Ed25519 公钥和私钥"
            exit 0
          fi

          echo "NETSPARKLE_ENABLED=true" >> "$GITHUB_ENV"
          echo "NETSPARKLE_APPCAST_URL=https://api.mslmc.cn/v4/download/update/appcast/xml/latest/appcast-${{ matrix.rid }}.xml" >> "$GITHUB_ENV"
          echo "NETSPARKLE_ED25519_PUBLIC_KEY=$SPARKLE_PUBLIC_KEY" >> "$GITHUB_ENV"

      - name: 导入 Developer ID Application 证书
        env:
          MACOS_CERTIFICATE_BASE64: ${{ secrets.MACOS_CERTIFICATE_BASE64 }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        run: |
          set -euo pipefail

          CERTIFICATE_PATH="$RUNNER_TEMP/mslx-developer-id.p12"
          KEYCHAIN_PATH="$RUNNER_TEMP/mslx-signing.keychain-db"
          KEYCHAIN_PASSWORD=$(openssl rand -hex 32)
          echo "::add-mask::$KEYCHAIN_PASSWORD"
          echo "KEYCHAIN_PATH=$KEYCHAIN_PATH" >> "$GITHUB_ENV"
          echo "KEYCHAIN_PASSWORD=$KEYCHAIN_PASSWORD" >> "$GITHUB_ENV"

          if [[ -z "$MACOS_CERTIFICATE_BASE64" || -z "$MACOS_CERTIFICATE_PASSWORD" ]]; then
            echo "::error::缺少 macOS 签名证书 Secrets"
            exit 1
          fi

          printf '%s' "$MACOS_CERTIFICATE_BASE64" | base64 --decode > "$CERTIFICATE_PATH"
          chmod 600 "$CERTIFICATE_PATH"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERTIFICATE_PATH" \
            -k "$KEYCHAIN_PATH" \
            -P "$MACOS_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign
          security set-key-partition-list \
            -S apple-tool:,apple:,codesign: \
            -s \
            -k "$KEYCHAIN_PASSWORD" \
            "$KEYCHAIN_PATH"

          security list-keychains -d user -s "$KEYCHAIN_PATH"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"
          SIGNING_IDENTITY=$(security find-identity -v -p codesigning "$KEYCHAIN_PATH" | sed -n 's/.*"\(Developer ID Application:.*\)"/\1/p' | head -n 1)
          if [[ -z "$SIGNING_IDENTITY" ]]; then
            echo "::error::证书中未找到 Developer ID Application 身份"
            exit 1
          fi

          echo "SIGNING_IDENTITY=$SIGNING_IDENTITY" >> "$GITHUB_ENV"
          rm -f "$CERTIFICATE_PATH"

      - name: 发布 macOS App 组件
        run: |
          set -euo pipefail

          DESKTOP_PUBLISH_DIR="./publish_desktop_temp"
          DAEMON_PUBLISH_DIR="./publish_daemon_temp"
          RUNTIME_PUBLISH_DIR="./publish_dotnet_runtime_temp"
          DIST_DIR="./dist"
          mkdir -p "$DIST_DIR"

          DOTNET_RUNTIME_VERSION=$(dotnet --list-runtimes | awk '$1 == "Microsoft.AspNetCore.App" { print $2 }' | tail -n 1)
          if [[ -z "$DOTNET_RUNTIME_VERSION" ]]; then
            echo "::error::无法确定 ASP.NET Core Runtime 版本"
            exit 1
          fi

          echo "共享 .NET Runtime 版本: $DOTNET_RUNTIME_VERSION"
          echo "DOTNET_RUNTIME_VERSION=$DOTNET_RUNTIME_VERSION" >> "$GITHUB_ENV"
          
          dotnet publish ./MSLX.Desktop/MSLX.Desktop.csproj \
            -c Release \
            -r ${{ matrix.rid }} \
            --self-contained false \
            -p:PublishSingleFile=true \
            -p:AppHostDotNetSearch=AppRelative \
            -p:AppHostRelativeDotNet=../Resources/dotnet \
            -p:DebugType=None \
            -p:DebugSymbols=false \
            -o "$DESKTOP_PUBLISH_DIR" \
            -p:IncludeNativeLibrariesForSelfExtract=true \
            -p:NetSparkleAppCastUrl="$NETSPARKLE_APPCAST_URL" \
            -p:NetSparkleEd25519PublicKey="$NETSPARKLE_ED25519_PUBLIC_KEY"

          dotnet publish ./MSLX.Daemon/MSLX.Daemon.csproj \
            -c Release \
            -r ${{ matrix.rid }} \
            --self-contained false \
            -p:PublishSingleFile=true \
            -p:AppHostDotNetSearch=AppRelative \
            -p:AppHostRelativeDotNet=../Resources/dotnet \
            -p:DebugType=None \
            -p:DebugSymbols=false \
            -o "$DAEMON_PUBLISH_DIR" \
            -p:IncludeNativeLibrariesForSelfExtract=true

          curl -fsSL https://dot.net/v1/dotnet-install.sh -o "$RUNNER_TEMP/dotnet-install.sh"
          bash "$RUNNER_TEMP/dotnet-install.sh" \
            --runtime aspnetcore \
            --version "$DOTNET_RUNTIME_VERSION" \
            --architecture "${{ matrix.arch }}" \
            --os osx \
            --install-dir "$RUNTIME_PUBLISH_DIR" \
            --no-path

      - name: 安装 create-dmg 工具
        run: brew install create-dmg

      - name: 生成 .dmg 安装包
        run: |
          set -euo pipefail

          BASE_NAME="MSLX-Desktop_${{ env.VERSION_NAME }}_${{ matrix.rid }}"
          DIST_DIR="./dist"
          
          echo "开始手动构建 macOS .app 结构..."
          APP_NAME="MSLX.app"
          APP_DIR="dmg_build/$APP_NAME"
          
          mkdir -p "$APP_DIR/Contents/MacOS"
          mkdir -p "$APP_DIR/Contents/Frameworks"
          mkdir -p "$APP_DIR/Contents/Resources/dotnet"
          
          # 1. MacOS 目录只放置可执行文件
          cp "./publish_desktop_temp/${{ matrix.bin }}" "$APP_DIR/Contents/MacOS/"
          cp "./publish_daemon_temp/MSLX-Daemon" "$APP_DIR/Contents/MacOS/"
          chmod +x "$APP_DIR/Contents/MacOS/${{ matrix.bin }}"
          chmod +x "$APP_DIR/Contents/MacOS/MSLX-Daemon"

          # 2. Desktop 与 Daemon 共用一套私有 .NET / ASP.NET Core Runtime
          DOTNET_ROOT="$APP_DIR/Contents/Resources/dotnet"
          NETCORE_SHARED_ROOT="$DOTNET_ROOT/shared"
          NETCORE_RUNTIME_DIR="$NETCORE_SHARED_ROOT/Microsoft.NETCore.App/$DOTNET_RUNTIME_VERSION"
          ASPNETCORE_RUNTIME_DIR="$NETCORE_SHARED_ROOT/Microsoft.AspNetCore.App/$DOTNET_RUNTIME_VERSION"
          HOSTFXR_DIR="$DOTNET_ROOT/host/fxr/$DOTNET_RUNTIME_VERSION"

          cp -R "./publish_dotnet_runtime_temp/host" "$DOTNET_ROOT/"
          cp -R "./publish_dotnet_runtime_temp/shared" "$DOTNET_ROOT/"
          mkdir -p "$APP_DIR/Contents/Resources/Licenses/dotnet"
          cp "./publish_dotnet_runtime_temp/LICENSE.txt" "$APP_DIR/Contents/Resources/Licenses/dotnet/"
          cp "./publish_dotnet_runtime_temp/ThirdPartyNotices.txt" "$APP_DIR/Contents/Resources/Licenses/dotnet/"

          test -f "$HOSTFXR_DIR/libhostfxr.dylib"
          test -d "$NETCORE_RUNTIME_DIR"
          test -d "$ASPNETCORE_RUNTIME_DIR"
          test -x "$NETCORE_RUNTIME_DIR/createdump"

          # 原生库放入 Frameworks，Resources 中保留 .NET 所需的目录和相对符号链接。
          mv "$HOSTFXR_DIR/libhostfxr.dylib" "$APP_DIR/Contents/Frameworks/libhostfxr.dylib"
          ln -s ../../../../../Frameworks/libhostfxr.dylib "$HOSTFXR_DIR/libhostfxr.dylib"
          for RUNTIME_LIBRARY in "$NETCORE_RUNTIME_DIR"/*.dylib; do
            LIBRARY_NAME=$(basename "$RUNTIME_LIBRARY")
            test ! -e "$APP_DIR/Contents/Frameworks/$LIBRARY_NAME"
            mv "$RUNTIME_LIBRARY" "$APP_DIR/Contents/Frameworks/$LIBRARY_NAME"
            ln -s "../../../../../Frameworks/$LIBRARY_NAME" "$RUNTIME_LIBRARY"
          done

          # createdump 是运行时辅助可执行程序，放入 MacOS 并保留 .NET 的标准探测路径。
          mv "$NETCORE_RUNTIME_DIR/createdump" "$APP_DIR/Contents/MacOS/createdump"
          ln -s ../../../../../MacOS/createdump "$NETCORE_RUNTIME_DIR/createdump"
          chmod +x "$APP_DIR/Contents/MacOS/createdump"

          test -L "$HOSTFXR_DIR/libhostfxr.dylib"
          test -d "$NETCORE_SHARED_ROOT/Microsoft.NETCore.App/$DOTNET_RUNTIME_VERSION"
          test -d "$NETCORE_SHARED_ROOT/Microsoft.AspNetCore.App/$DOTNET_RUNTIME_VERSION"
          test -x "$APP_DIR/Contents/MacOS/createdump"
          test "$(find "$APP_DIR/Contents/MacOS" -mindepth 1 -maxdepth 1 -type f | wc -l | tr -d ' ')" = "3"
          test -z "$(find "$APP_DIR/Contents/MacOS" -mindepth 1 -maxdepth 1 -type f ! -perm -111 -print -quit)"
          
          # 3. 拷贝图标文件及设置本地化资源目录
          cp ./MSLX.Desktop/Assets/icon.icns "$APP_DIR/Contents/Resources/icon.icns"
          mkdir -p "$APP_DIR/Contents/Resources/zh_CN.lproj"
          mkdir -p "$APP_DIR/Contents/Resources/zh-Hans.lproj"
          
          # 4. 写入标准的 Info.plist 文件
          cat <<EOF > "$APP_DIR/Contents/Info.plist"
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>${{ matrix.bin }}</string>
              <key>CFBundleDevelopmentRegion</key>
              <string>zh_CN</string>
              <key>CFBundleLocalizations</key>
              <array>
                  <string>zh_CN</string>
                  <string>zh-Hans</string>
              </array>
              <key>CFBundleIconFile</key>
              <string>icon.icns</string>
              <key>CFBundleIdentifier</key>
              <string>cn.mslmc.mslx</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleName</key>
              <string>MSLX</string>
              <key>CFBundleDisplayName</key>
              <string>MSLX</string>
              <key>CFBundleVersion</key>
              <string>${{ env.BUNDLE_VERSION }}</string>
              <key>CFBundleShortVersionString</key>
              <string>${{ env.BUNDLE_SHORT_VERSION }}</string>
          </dict>
          </plist>
          EOF

          test "$(plutil -extract CFBundlePackageType raw -o - "$APP_DIR/Contents/Info.plist")" = "APPL"

          echo "开始签名 $APP_NAME..."
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          # 先签共享运行时中的原生动态库，再签辅助程序、应用可执行文件和外层 App。
          while IFS= read -r -d '' FILE_PATH; do
            if file "$FILE_PATH" | grep -q 'Mach-O'; then
              echo "签名运行时: $FILE_PATH"
              codesign \
                --force \
                --timestamp \
                --options runtime \
                --keychain "$KEYCHAIN_PATH" \
                --sign "$SIGNING_IDENTITY" \
                "$FILE_PATH"
            fi
          done < <(find "$APP_DIR/Contents/Frameworks" -type f -print0)

          codesign \
            --force \
            --timestamp \
            --options runtime \
            --keychain "$KEYCHAIN_PATH" \
            --sign "$SIGNING_IDENTITY" \
            "$APP_DIR/Contents/MacOS/createdump"

          for FILE_PATH in \
            "$APP_DIR/Contents/MacOS/${{ matrix.bin }}" \
            "$APP_DIR/Contents/MacOS/MSLX-Daemon"; do
            if file "$FILE_PATH" | grep -q 'Mach-O'; then
              echo "签名可执行文件: $FILE_PATH"
              codesign \
                --force \
                --timestamp \
                --options runtime \
                --entitlements ./MSLX.Desktop/macOS.entitlements \
                --keychain "$KEYCHAIN_PATH" \
                --sign "$SIGNING_IDENTITY" \
                "$FILE_PATH"
            fi
          done

          codesign \
            --force \
            --timestamp \
            --options runtime \
            --entitlements ./MSLX.Desktop/macOS.entitlements \
            --keychain "$KEYCHAIN_PATH" \
            --sign "$SIGNING_IDENTITY" \
            "$APP_DIR"

          codesign --verify --deep --strict --verbose=2 "$APP_DIR"
          
          cd dmg_build
          
          # create-dmg 对 .app 进行打包，加入极简美化参数
          create-dmg \
            --volname "MSLX Installer" \
            --volicon "../MSLX.Desktop/Assets/icon.icns" \
            --window-pos 200 120 \
            --window-size 500 300 \
            --icon-size 120 \
            --text-size 14 \
            --icon "$APP_NAME" 120 120 \
            --hide-extension "$APP_NAME" \
            --app-drop-link 380 120 \
            "../$DIST_DIR/$BASE_NAME.dmg" \
            "$APP_NAME"
            
          cd ..
          
          # 将生成的包路径保存到环境变量
          echo "ASSET_PATH=./dist/$BASE_NAME.dmg" >> $GITHUB_ENV

      - name: 签名并公证 DMG
        env:
          APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY_BASE64 }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
        run: |
          set -euo pipefail

          if [[ -z "$APPLE_API_KEY_BASE64" || -z "$APPLE_API_KEY_ID" || -z "$APPLE_API_ISSUER_ID" ]]; then
            echo "::error::缺少 App Store Connect API Key Secrets"
            exit 1
          fi

          API_KEY_PATH="$RUNNER_TEMP/AuthKey_${APPLE_API_KEY_ID}.p8"
          NOTARY_RESULT_PATH="$RUNNER_TEMP/notary-result.json"
          echo "APPLE_API_KEY_PATH=$API_KEY_PATH" >> "$GITHUB_ENV"
          printf '%s' "$APPLE_API_KEY_BASE64" | base64 --decode > "$API_KEY_PATH"
          chmod 600 "$API_KEY_PATH"

          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          codesign \
            --force \
            --timestamp \
            --keychain "$KEYCHAIN_PATH" \
            --sign "$SIGNING_IDENTITY" \
            "${{ env.ASSET_PATH }}"
          codesign --verify --verbose=2 "${{ env.ASSET_PATH }}"

          set +e
          xcrun notarytool submit "${{ env.ASSET_PATH }}" \
            --key "$API_KEY_PATH" \
            --key-id "$APPLE_API_KEY_ID" \
            --issuer "$APPLE_API_ISSUER_ID" \
            --wait \
            --output-format json > "$NOTARY_RESULT_PATH"
          SUBMIT_EXIT=$?
          set -e

          cat "$NOTARY_RESULT_PATH"
          SUBMISSION_ID=$(jq -r '.id // empty' "$NOTARY_RESULT_PATH" 2>/dev/null || true)
          SUBMISSION_STATUS=$(jq -r '.status // empty' "$NOTARY_RESULT_PATH" 2>/dev/null || true)

          if [[ "$SUBMIT_EXIT" -ne 0 || -z "$SUBMISSION_ID" || "$SUBMISSION_STATUS" != "Accepted" ]]; then
            if [[ -n "$SUBMISSION_ID" ]]; then
              xcrun notarytool log "$SUBMISSION_ID" \
                --key "$API_KEY_PATH" \
                --key-id "$APPLE_API_KEY_ID" \
                --issuer "$APPLE_API_ISSUER_ID" || true
            fi
            echo "::error::Apple 公证失败，Submission ID: ${SUBMISSION_ID:-unknown}，状态: ${SUBMISSION_STATUS:-unknown}"
            exit 1
          fi

          echo "Apple 公证已通过，Submission ID: $SUBMISSION_ID"

          xcrun stapler staple "${{ env.ASSET_PATH }}"
          xcrun stapler validate "${{ env.ASSET_PATH }}"

      - name: 生成 NetSparkle AppCast
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.NETSPARKLE_ED25519_PRIVATE_KEY }}
          SPARKLE_PUBLIC_KEY: ${{ secrets.NETSPARKLE_ED25519_PUBLIC_KEY }}
        run: |
          set -euo pipefail

          if [[ "$NETSPARKLE_ENABLED" != "true" ]]; then
            echo "::notice::当前构建未启用 NetSparkle，跳过 AppCast 生成"
            exit 0
          fi

          CHANGELOG_DIR="$RUNNER_TEMP/netsparkle-changelogs-${{ matrix.rid }}"
          CHANGELOG_PATH="$CHANGELOG_DIR/$BUNDLE_SHORT_VERSION.md"
          mkdir -p "$CHANGELOG_DIR"

          CHANGELOG_CONTENT=""
          CHANGELOG_ATTEMPT=1
          CHANGELOG_DEADLINE=$((SECONDS + 1800))

          while true; do
            CHANGELOG_RESPONSE=""
            CHANGELOG_CONTENT=""
            CHANGELOG_REMAINING=$((CHANGELOG_DEADLINE - SECONDS))

            if (( CHANGELOG_REMAINING <= 0 )); then
              echo "::warning::等待版本 $BUNDLE_SHORT_VERSION 的更新日志已达到 30 分钟，按无更新日志继续生成 AppCast"
              break
            fi

            CHANGELOG_QUERY_TIMEOUT=30
            if (( CHANGELOG_REMAINING < CHANGELOG_QUERY_TIMEOUT )); then
              CHANGELOG_QUERY_TIMEOUT=$CHANGELOG_REMAINING
            fi

            if CHANGELOG_RESPONSE=$(curl \
                --fail \
                --silent \
                --show-error \
                --connect-timeout 10 \
                --max-time "$CHANGELOG_QUERY_TIMEOUT" \
                "https://api.mslmc.cn/v4/software/changelogs?software=MSLX-Desktop"); then
              CHANGELOG_CONTENT=$(jq -r \
                --arg version "$BUNDLE_SHORT_VERSION" \
                'if .code == 200 then first(.data[]? | select(.version == $version) | .changes) // empty else empty end' \
                <<< "$CHANGELOG_RESPONSE" 2>/dev/null || true)
            else
              echo "::warning::第 $CHANGELOG_ATTEMPT 次更新日志查询失败"
            fi

            if [[ -n "${CHANGELOG_CONTENT//[[:space:]]/}" ]]; then
              echo "::notice::已获取版本 $BUNDLE_SHORT_VERSION 的更新日志，查询次数: $CHANGELOG_ATTEMPT"
              break
            fi

            CHANGELOG_REMAINING=$((CHANGELOG_DEADLINE - SECONDS))
            if (( CHANGELOG_REMAINING <= 0 )); then
              echo "::warning::等待版本 $BUNDLE_SHORT_VERSION 的更新日志已达到 30 分钟，按无更新日志继续生成 AppCast"
              break
            fi

            echo "::notice::更新日志 API 中尚无版本 $BUNDLE_SHORT_VERSION 的有效内容，60 秒后执行第 $((CHANGELOG_ATTEMPT + 1)) 次查询"
            sleep 60
            ((CHANGELOG_ATTEMPT += 1))
          done

          if [[ -n "${CHANGELOG_CONTENT//[[:space:]]/}" ]]; then
            printf '%s\n' "$CHANGELOG_CONTENT" > "$CHANGELOG_PATH"
          else
            echo "::warning::更新日志 API 中没有版本 $BUNDLE_SHORT_VERSION 的有效内容，继续生成 AppCast"
          fi

          TOOL_DIR="$RUNNER_TEMP/netsparkle-tools"
          dotnet tool install NetSparkleUpdater.Tools.AppCastGenerator \
            --version 2.9.0 \
            --tool-path "$TOOL_DIR"

          "$TOOL_DIR/netsparkle-generate-appcast" \
            --single-file "${{ env.ASSET_PATH }}" \
            --file-version "$BUNDLE_SHORT_VERSION" \
            --os "macos-${{ matrix.arch }}" \
            --base-url "https://api.mslmc.cn/v4/download/update/appcast/dmg/$VERSION_NAME" \
            --change-log-path "$CHANGELOG_DIR" \
            --appcast-output-directory ./dist \
            --output-file-name "appcast-${{ matrix.rid }}" \
            --link-tag "$NETSPARKLE_APPCAST_URL" \
            --product-name "MSLX" \
            --description-tag "MSLX macOS 更新" \
            --human-readable true

          APPCAST_PATH="./dist/appcast-${{ matrix.rid }}.xml"
          APPCAST_SIGNATURE_PATH="$APPCAST_PATH.signature"
          test -f "$APPCAST_PATH"
          test -f "$APPCAST_SIGNATURE_PATH"

          echo "APPCAST_PATH=$APPCAST_PATH" >> "$GITHUB_ENV"
          echo "APPCAST_SIGNATURE_PATH=$APPCAST_SIGNATURE_PATH" >> "$GITHUB_ENV"

      - name: 清理签名凭据
        if: always()
        run: |
          if [[ -n "${APPLE_API_KEY_PATH:-}" ]]; then
            rm -f "$APPLE_API_KEY_PATH"
          fi
          rm -f "$RUNNER_TEMP/mslx-developer-id.p12"
          if [[ -n "${KEYCHAIN_PATH:-}" ]]; then
            security delete-keychain "$KEYCHAIN_PATH" || true
          fi

      - name: 上传到 GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ASSET_PATH }}
          tag_name: ${{ env.VERSION_NAME }}
          prerelease: ${{ env.VERSION_NAME == 'beta' }}

      - name: 上传 NetSparkle AppCast 到 GitHub Release
        if: env.APPCAST_PATH != '' && env.APPCAST_SIGNATURE_PATH != ''
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ env.APPCAST_PATH }}
            ${{ env.APPCAST_SIGNATURE_PATH }}
          tag_name: ${{ env.VERSION_NAME }}
          prerelease: ${{ env.VERSION_NAME == 'beta' }}

      - name: 同步上传到 CNB Release
        env:
          CNB_REPO_SLUG: "mslmc/mslx"
          CNB_TOKEN: ${{ secrets.CNB_MAIN_REPO_TOKEN }}
          TAG_NAME: ${{ env.VERSION_NAME }}
        run: |
          echo "正在准备同步 macOS DMG 及 AppCast 到国内 CNB 主节点..."
          
          if [[ "$TAG_NAME" == "beta" ]]; then
            echo "当前为手动测试构建，跳过 CNB 同步。"
            exit 0
          fi

          if [ ! -f "${{ env.ASSET_PATH }}" ]; then
            echo "❌ 错误：未找到生成的 dmg 压缩包文件！"
            exit 1
          fi

          curl -s \
            -H "Authorization: token $CNB_TOKEN" \
            -H "Accept: application/json" \
            "https://api.cnb.cool/${CNB_REPO_SLUG}/-/git/branches?page_size=1" > branches_res.json
          
          CNB_TARGET_BRANCH=$(jq -r 'if type == "array" then .[0].name else .data[0].name? // .items[0].name? // "main" end' branches_res.json)
          
          if [ -z "$CNB_TARGET_BRANCH" ] || [ "$CNB_TARGET_BRANCH" = "null" ]; then
            CNB_TARGET_BRANCH="main" # 兜底安全分支
          fi
          echo "💡 锁定 CNB 侧分支为: $CNB_TARGET_BRANCH"

          curl -s -X POST \
            -H "Authorization: token $CNB_TOKEN" \
            -H "Content-Type: application/json" \
            -H "Accept: application/json" \
            -d "{\"tag_name\": \"${TAG_NAME}\", \"target_commitish\": \"${CNB_TARGET_BRANCH}\", \"name\": \"Release ${TAG_NAME}\", \"body\": \"Synchronized from GitHub Main Repo\"}" \
            "https://api.cnb.cool/${CNB_REPO_SLUG}/-/releases" > create_res.json

          RELEASE_ID=$(jq -r '.id // empty' create_res.json)

          # 并发矩阵节点查缺补漏列表反查
          if [ -z "$RELEASE_ID" ] || [ "$RELEASE_ID" = "null" ]; then
            echo "正在反查已存在的 Release 标识符..."
            curl -s \
              -H "Authorization: token $CNB_TOKEN" \
              -H "Accept: application/json" \
              "https://api.cnb.cool/${CNB_REPO_SLUG}/-/releases" > list_res.json
            
            RELEASE_ID=$(jq -r --arg tag "$TAG_NAME" 'if type == "array" then .[] else .data[]? // .items[]? // .[]? end | select(.tag_name==$tag) | .id' list_res.json | head -n 1)
          fi

          if [ -z "$RELEASE_ID" ] || [ "$RELEASE_ID" = "null" ]; then
            echo "❌ 错误：未能获取到 CNB 的 Release ID。返回内容："
            cat create_res.json
            echo "列表反查内容："
            cat list_res.json
            exit 1
          fi

          echo "✅ 成功对接主仓 Release，ID 为: $RELEASE_ID"

          upload_asset_to_cnb() {
            local TARGET_FILE="$1"
            if [ -z "$TARGET_FILE" ] || [ ! -f "$TARGET_FILE" ]; then
              return 0
            fi

            local FILE_NAME
            FILE_NAME=$(basename "$TARGET_FILE")
            # macOS runner 下获取文件大小的 stat 参数是 -f%z 而非 Linux 的 -c%s
            local FILE_SIZE
            FILE_SIZE=$(stat -f%z "$TARGET_FILE")

            echo "正在获取附件 $FILE_NAME 上传地址..."
            curl -s -X POST \
              -H "Authorization: token $CNB_TOKEN" \
              -H "Content-Type: application/json" \
              -H "Accept: application/json" \
              -d "{\"asset_name\": \"${FILE_NAME}\", \"size\": ${FILE_SIZE}, \"overwrite\": true}" \
              "https://api.cnb.cool/${CNB_REPO_SLUG}/-/releases/${RELEASE_ID}/asset-upload-url" > url_res.json

            local UPLOAD_URL
            local VERIFY_URL
            UPLOAD_URL=$(jq -r '.upload_url // empty' url_res.json)
            VERIFY_URL=$(jq -r '.verify_url // empty' url_res.json)

            if [ -z "$UPLOAD_URL" ] || [ "$UPLOAD_URL" = "null" ]; then
              echo "❌ 错误：无法获取 $FILE_NAME 的上传地址。返回内容："
              cat url_res.json
              exit 1
            fi

            echo "正在把文件 $FILE_NAME 推送到预签名高速通道..."
            curl -s -X PUT -T "$TARGET_FILE" "$UPLOAD_URL"

            echo "正在确认附件 $FILE_NAME 上传状态..."
            local CONFIRM_CODE
            CONFIRM_CODE=$(curl -s -o confirm_res.json -w "%{http_code}" -X POST \
              -H "Authorization: token $CNB_TOKEN" \
              -H "Accept: application/json" \
              "${VERIFY_URL}?ttl=0")

            if [ "$CONFIRM_CODE" -eq 200 ] || [ "$CONFIRM_CODE" -eq 201 ]; then
              echo "🎉 [${{ matrix.rid }}] 附件 $FILE_NAME 已成功关联到 CNB 主仓 Release 页面！"
            else
              echo "❌ 错误：确认附件 $FILE_NAME 失败。状态码: ${CONFIRM_CODE}，返回内容："
              cat confirm_res.json
              exit 1
            fi
          }

          upload_asset_to_cnb "${{ env.ASSET_PATH }}"

          if [ -n "${APPCAST_PATH:-}" ]; then
            upload_asset_to_cnb "${APPCAST_PATH}"
          fi

          if [ -n "${APPCAST_SIGNATURE_PATH:-}" ]; then
            upload_asset_to_cnb "${APPCAST_SIGNATURE_PATH}"
          fi
